
#+TITLE: Homework 2
#+AUTHOR: Bray Moll
#+OPTIONS: toc:nil
#+SETUPFILE: ~/masters/bray-standard-latex-export.org

* Exercise 1
*** Given:
a) THe datafile compstats1.RData \\

*** Find:
a) Load the data file \\
b) Using the vecor of ages create a factor that has levels Minor or Adult where
any observation greter than or equal to 18 qualifies as an adult. Make sure the
order of the levels is Minor first and Adult second \\


*** Solution
#+ATTR_LATEX :options frame=single
#+BEGIN_SRC R :results output :exports both :session R-session 
  rm(list=ls())
  library(forcats)
  setwd("~/masters/computational-statistics/hw/hw1")
  #Exercise 1
  # Load the datafile compstats1.RData using load
  compstats = load("compstats1.RData")
  adult <- as.factor(ifelse(ages<18,"Minor","Adult"))

  # Ensure order is minor first, adult second
  adult_relevel <- fct_relevel(adult, "Minor")

#+END_SRC

#+RESULTS:
: Error in readChar(con, 5L, useBytes = TRUE) : cannot open the connection
: In addition: Warning message:
: In readChar(con, 5L, useBytes = TRUE) :
:   cannot open compressed file 'compstats1.RData', probable reason 'No such file or directory'
: Error: object 'ages' not found
: Error: object 'adult' not found
